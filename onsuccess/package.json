{
  "name": "robot-on-success",
  "author": "Bruno Souza <bruno.souza@ecs.com.br>",
  "description": "Lambda save status on DB when success call",
  "version": "1.0.0",
  "devDependencies": {
    "serverless": "^1.49.0",
    "serverless-python-requirements": "^5.0.0"
  },
  "scripts": {
    "build": "npx serverless package --package ../deploy/on-success",
    "deploy": "npx sls deploy"
  }
}
