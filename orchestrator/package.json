{
  "name": "robot-orchestrator",
  "author": "Bruno Souza <bruno.souza@ecs.com.br>",
  "description": "Lambda which orchestrate awa connect start outbound call",
  "version": "1.0.0",
  "devDependencies": {
    "serverless": "^1.33.2"
  },
  "scripts": {
    "build": "npx serverless package --package ../deploy/orchestrator",
    "deploy": "npx sls deploy"
  },
  "dependencies": {
    "serverless-python-requirements": "^4.2.5"
  }
}
